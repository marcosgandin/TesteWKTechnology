unit Controller.Produtos;

interface

uses
  StrUtils,
  SysUtils,
  Model.Produtos;

type
  TControllerProdutos = class
  public
    function Salvar(const objProduto: TProdutos): string;
    function GetNome(const objProduto: TProdutos): string;
    function GetPreco(const objProduto: TProdutos): currency;
  end;

implementation

{ TControllerProdutos }

function TControllerProdutos.GetNome(const objProduto: TProdutos): string;
begin

end;

function TControllerProdutos.GetPreco(const objProduto: TProdutos): currency;
begin

end;

function TControllerProdutos.Salvar(const objProduto: TProdutos): string;
begin
  Result := EmptyStr;

  if objProduto.Descricao = EmptyStr then
    Result := 'Nome do produto é obrigatório!';

  if objProduto.VlVenda <= 0 then
    Result := 'Valor do produto é obrigatório!';

  if Result = EmptyStr then
    Result := objProduto.Salvar(objProduto);
end;

end.
